node_modules/
data/

# secrets — APP_PASSWORD, TURSO_AUTH_TOKEN. Bun auto-loads .env, so anything
# here reaches the app AND the test suite. .env.example is the tracked template.
.env
.env.*
!.env.example
*.db
*.db-shm
*.db-wal
.DS_Store

# playwright
e2e-report/
test-results/

# typescript incremental build cache
*.tsbuildinfo
